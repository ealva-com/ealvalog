package ealvalog.impl;

import org.junit.Test;

import static ealvalog.impl.ExtRecordFormatter.DATE_TIME_FORMAT;
import static org.hamcrest.MatcherAssert.assertThat;
import static org.hamcrest.Matchers.equalTo;
import static org.hamcrest.Matchers.is;

import java.util.Locale;

/**
 * Test LogRecord formatting
 *
 * Created by Eric A. Snell on 3/5/17.
 */
public class ExtRecordFormatterTest {

  @Test
  public void deleteMe() {
//    assertThat(String.format(Locale.getDefault(), DATE_TIME_FORMAT, 0, 0, 0, 0, System.currentTimeMillis()), is(equalTo("")));
  }

}
